name: GPT-OS Engine

on:
  push:
    paths:
      - 'events/**'
  repository_dispatch:
    types: [run_analysis]

jobs:
  process:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@3
      - run: echo "analysis" > memory/results/result.json
