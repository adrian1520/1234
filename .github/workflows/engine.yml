name: GPT-OS Engine

on:
  push:
    paths:
      - 'events/**'
  repository_dispatch:
    types: [run_analysis]

jobs:
  process:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Read event
        run: echo "Processing events..."

      - name: Done
        run: echo "GPT OS RUNNING"