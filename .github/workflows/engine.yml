name: GPT-OS Engine

on:
  push:
    paths:
      - 'events/**'

jobs:
  process:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Read event
        run: echo "Processing events..."

      - name: Done
        run: echo "GOT OS RUNNING"